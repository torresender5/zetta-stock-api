model Client {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  phone     String
  address   String
  document  String   @unique
  createdAt DateTime @default(now())
  sales     Sale[]
  invoices  Invoice[]
}
